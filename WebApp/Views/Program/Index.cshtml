@model IEnumerable<Entities.Concrete.Program>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/ManagerPanel/_Layout.cshtml";
}

<br />

<p>
    <a href="/Program/Create" class="btn btn-success">Create</a>
</p>
<table class="table" id="tablo">
    <thead>
        <tr>
            <th>ID</th>
            <th>PROGRAM</th>
            <th>UPDATE</th>
            <th>DELETE</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.Id</td>
                <td>@item.Description</td>
                <td><a href="/Program/Bring/@item.Id" class="btn btn-primary">Update</a></td>
                <td><a href="/Program/Delete/@item.Id" class="btn btn-danger">Delete</a></td>
            </tr>
        }
    </tbody>

</table>

